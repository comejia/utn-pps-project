package flow.pageActions;

import ch.qos.logback.classic.Logger;
import org.slf4j.LoggerFactory;

public class ActionPageBase {
    protected Logger logger = (Logger) LoggerFactory.getLogger(String.valueOf(ActionPageBase.class));
}
